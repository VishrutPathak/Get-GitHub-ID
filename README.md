# Get-GitHub-ID
A simple web application that allows users to enter a GitHub username and generate a link to their profile. The app dynamically updates the anchor tag to redirect users to the corresponding GitHub profile.
